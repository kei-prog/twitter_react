const CreateAccountButton = () => {
  return (
    <button className="bg-blue-500 hover:bg-blue-600 text-white font-bold w-60 py-2 rounded-full">
      アカウントを作成
    </button>
  );
};

export default CreateAccountButton;
